# Locked Constraints

Approved via plan acceptance (Freelance Portfolio Strategy — Amr Samy).  
Values below are the plan defaults unless later revised in writing.

| Constraint | Decision |
|---|---|
| Weekly capacity (next 3 months) | **10–15 hours/week** while employed |
| Primary demo stack | **Java 21 + Spring Boot** |
| Markets | **Upwork (primary USD)** + **Khamsat (MENA)** + Freelancer |
| Flagship build order | **CaseFlow → DispatchGrid → RelateAI** |
| Project count | **3 flagships + portfolio site** (not 5 full apps) |
| Public career metrics | **Anonymized only** — use `~45 min → ~2 min` and `20M+ messages` without naming employers/clients or proprietary product names |
| Differentiator theme | **Measurable Systems** |
| Architecture default | Modular monolith (Clean/Hexagonal packages); RabbitMQ + PostgreSQL; Redis when justified |
| Frontend role | Thin demo UI only (Next.js/React) — not a frontend-specialist brand |

## Implications

- If capacity drops to 5–8 hrs/week: ship CaseFlow + portfolio site only; delay DispatchGrid.
- Do not lead freelance profiles with Rust, confidential computing, FoundationDB, or Micronaut until public demos exist.
- GraphQL / Neo4j / Kafka only when a project truly requires them (RelateAI may use graph modeling; default CaseFlow/DispatchGrid = REST + Postgres).
